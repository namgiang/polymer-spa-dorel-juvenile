/**
 * @constant
 * @name CONFIG
 * @description all constants for the application to use 
 */
var CONFIG = {
  CMS_URL: "{{CMS_URL}}",                // Used for endpoints to CMS
  DIO_API_URL: "{{DIO_API_URL}}",        // Used for the multi management tool
  BRAND_NAME: "{{BRAND_NAME}}",          // Used for brand name and theme switcher
  LOCALE: {
    COUNTRY: '{{COUNTRY}}',
    LANGUAGE: '{{LANG}}',
    LANG_CONTENT: '{{LOCALE}}',
    LINK_ALT: '{{ALT_LINK}}'
  },
};